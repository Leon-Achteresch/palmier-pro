{
  "name": "palmier-pro",
  "private": true,
  "scripts": {
    "install": "scripts/bundle.sh release && rm -rf /Applications/PalmierPro.app && cp -R .build/PalmierPro.app /Applications/ && echo 'PalmierPro installed to /Applications'"
  }
}
