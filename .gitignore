.DS_Store
.swiftpm/
.build/
.vscode/

# Xcode
xcuserdata/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM
timeline.xctimeline
playground.xcworkspace

wwdc/

# Secrets
.env
.env.*
*.pem
*.p12
*.p8
AuthKey_*.p8
credentials.json
secrets.json
*.local
*.provisionprofile

# Motion runtime toolchain (the built runtime itself is checked in under Sources/)
web/node_modules/

# React Native toolchain (rebuild with native/rn/build-host.sh; the JS bundle is checked in)
native/rn/node_modules/
native/rn/build/
native/rn/macos/Pods/
native/rn/macos/build/
native/rn/macos/PalmierRN.xcodeproj/
native/rn/macos/PalmierRN.xcworkspace/
native/rn/macos/Podfile.lock

# mcpb shim dependencies (vendored only inside the packed .mcpb)
mcpb/server/node_modules/
mcpb/server/package-lock.json

# CLIP models
models/*/.venv/
models/*/build*/
models/*/checkpoint*
models/*/convert*.log

AUDIO_SUITE_*
graphify-out/
